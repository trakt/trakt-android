<resources>
  <!--
    On API 31+ the system splash screen paints windowSplashScreenBackground rather than
    windowBackground, so it needs the same day/night colour or the splash keeps the platform
    default while the app below it is themed.
  -->
  <style name="Theme.Trakt" parent="Theme.MaterialComponents.DayNight.NoActionBar">
    <item name="android:windowBackground">@color/window_background</item>
    <item name="android:windowSplashScreenBackground">@color/window_background</item>
  </style>
</resources>
